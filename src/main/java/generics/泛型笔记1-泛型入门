泛型笔记1-泛型入门
在没有泛型之前，一旦把一个对象"丢进"Java集合中，集合就会忘记对象的类型，把所有对象当成Object类型处理。
这样从集合中取出对象之后，就需要进行强转。（代码臃肿、容易引起ClassCastException异常）

有泛型之后，集合就可以记住集合中元素的类型，并可以在编译时检查集合中元素的类型。

9.1 泛型入门
9.1.1 编译时不检查类型的异常
可能发生ClassCastException异常

9.1.2 手动实现编译时检查类型

9.1.3 使用泛型

9.1.4 "菱形"语法（jdk1.7+）
Java7以后，构造器后不需要带完整的泛型信息，只要给出一对尖括号"<>"即可。